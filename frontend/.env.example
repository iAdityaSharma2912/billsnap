# BillSnap frontend — environment variables (optional)
# The API base URL is currently hardcoded to http://localhost:8000 in
# src/services/api.ts since the backend always runs locally.
# This file is a placeholder if you later want to make it configurable.
VITE_API_BASE_URL=http://localhost:8000
